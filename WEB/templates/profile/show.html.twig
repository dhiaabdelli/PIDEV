{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
    <h1>Profile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profile.id }}</td>
            </tr>
            <tr>
                <th>Emailcompte</th>
                <td>{{ profile.emailcompte }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ profile.sexe }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ profile.age }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ profile.poids }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ profile.taille }}</td>
            </tr>
            <tr>
                <th>Objectif</th>
                <td>{{ profile.objectif }}</td>
            </tr>
            <tr>
                <th>Nbrepas</th>
                <td>{{ profile.nbrepas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('profile_index') }}">back to list</a>

    <a href="{{ path('profile_edit', {'id': profile.id}) }}">edit</a>

    {{ include('profile/_delete_form.html.twig') }}
{% endblock %}
