<form method="post" action="{{ path('programmealimentaire_delete', {'idprofile': programmealimentaire.idprofile}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programmealimentaire.idprofile) }}">
    <button class="btn">Delete</button>
</form>
